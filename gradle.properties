# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G

# Mod Properties
mod_version = 9.0.0-alpha.3
maven_group = dex.autoswitch
archives_base_name = autoswitch

# Modmenu version is ignored
modmenu_version = 1.14.6+build.31
modupdater_version = 1.1.9+1.16.1

# Acceptable MC version range
# Added to mod.json and used for Modrinth publishing -alpha.22.45.a
range = >=1.20.5-alpha.24.10.a

# All ignored (overwritten by Outlet)

# Minimum Java version
# Added to mod.json and used for curseforge publishing
java = 17

minecraft_version=24w09a
yarn_mappings=24w09a+build.6
loader_version=0.15.7

#Fabric api
fabric_version=0.96.5+1.20.5
